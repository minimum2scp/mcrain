machine:
  ruby:
    version: 2.2.2
  services:
    - docker

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  pre:
    - gem install bundler --version '~> 1.9.9'

